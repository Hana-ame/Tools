package main

import (
	"flag"
)

func main() {
	// 定义命令行参数
	// wsUrl := flag.String("u", "ws://file.moonchan.xyz/ws/server", "")
	// dstUrl := flag.Int("l", "localhost:8080", "")

	// 解析命令行参数
	flag.Parse()

	// bus := wsreverse.NewWsClient(*wsUrl, time.Second)

	// client := mymux.NewClient(bus, 5)
	// go client.ReadDaemon()

	// for {

	// }

}
